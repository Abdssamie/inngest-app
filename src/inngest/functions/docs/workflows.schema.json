{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Workflow Action Definition",
  "description": "Defines a custom action that can be used in a workflow.",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The action's unique identifier (serves as the 'kind'). This is used to look up the action's definition."
      },
      "name": {
        "type": "string",
        "description": "The human-readable name of the action, displayed in the UI."
      },
      "description": {
        "type": "string",
        "description": "A short, optional description of what the action does."
      },
      "trigger_type": {
        "type": "string",
        "enum": ["SCHEDULE", "EVENT"]
      },
      "trigger": {
        "type": "string",
        "description": "The trigger for the workflow."
      }

    },
    "required": ["id", "name", "trigger_type", "trigger"]
  }
}
